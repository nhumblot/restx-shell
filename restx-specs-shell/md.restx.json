{
    "parent": "io.restx:restx-shell-parent:${restx.shell.version}",
    "module": "io.restx:restx-specs-shell:${restx.shell.version}",

    "properties": {
        "@files": ["../restx.build.properties.json"]
    },

    "dependencies": {
        "compile": [
            "io.restx:restx-factory:${restx.version}",
            "io.restx:restx-shell:${restx.shell.version}",
            "io.restx:restx-core-shell:${restx.shell.version}",
            "io.restx:restx-specs-server:${restx.version}"
        ]
    }
}
